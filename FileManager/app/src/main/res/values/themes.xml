<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FileManager" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blueColor</item>
        <item name="colorPrimaryVariant">@color/lightGrayColor</item>
        <item name="colorOnPrimary">@color/blueColor</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/darkGrayColor</item>
        <item name="colorSecondaryVariant">@color/blueColor</item>
        <item name="colorOnSecondary">@color/blueColor</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="windowNoTitle"> true</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>